# BookStack

Documentation wiki with MariaDB.

Required local images:

- `bookstack:local`
- `mariadb:local`

Run:

```bash
dockan compose up
dockan compose health
```

Open:

```text
http://127.0.0.1:8087
```
