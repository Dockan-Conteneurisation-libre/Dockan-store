# n8n

Workflow automation with PostgreSQL.

Required local images:

- `n8n:local`
- `postgres:local`

Run:

```bash
dockan compose up
dockan compose health
```

Open:

```text
http://127.0.0.1:5678
```
