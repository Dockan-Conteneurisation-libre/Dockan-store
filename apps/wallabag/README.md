# Wallabag

Read-it-later app with PostgreSQL.

Required local images:

- `wallabag:local`
- `postgres:local`

Run:

```bash
dockan compose up
dockan compose health
```

Open:

```text
http://127.0.0.1:8086
```
