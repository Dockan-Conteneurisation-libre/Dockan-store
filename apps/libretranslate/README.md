# LibreTranslate

Local translation API and web UI.

Required local image:

- `libretranslate:local`

Run:

```bash
dockan compose up
dockan compose health
```

Open:

```text
http://127.0.0.1:5000
```
