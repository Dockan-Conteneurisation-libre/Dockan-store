# HedgeDoc

Collaborative markdown notes with PostgreSQL.

Required local images:

- `hedgedoc:local`
- `postgres:local`

Run:

```bash
dockan compose up
dockan compose health
```

Open:

```text
http://127.0.0.1:3003
```
